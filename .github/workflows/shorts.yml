name: AI Shorts Generator

on:
  workflow_dispatch:

  schedule:
    - cron: "0 9 * * *"
    - cron: "0 15 * * *"
    - cron: "0 21 * * *"

jobs:
  generate:

    runs-on: ubuntu-latest

    timeout-minutes: 30

    steps:

      # ==========================
      # DOWNLOAD PROJECT
      # ==========================

      - name: Download project
        uses: actions/checkout@v4


      # ==========================
      # PYTHON
      # ==========================

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"


      # ==========================
      # DENO
      # ==========================

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x


      # ==========================
      # FFMPEG
      # ==========================

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg


      # ==========================
      # WHISPER CACHE
      # ==========================

      - name: Cache Whisper model
        uses: actions/cache@v4
        with:
          path: ~/.cache/whisper
          key: whisper-tiny-v1


      # ==========================
      # PYTHON PACKAGES
      # ==========================

      - name: Install packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      # ==========================
      # UPDATE YT-DLP
      # ==========================

      - name: Update yt-dlp
        run: |
          python -m pip install --upgrade yt-dlp
          yt-dlp --version


      # ==========================
      # PREPARE DIRECTORIES
      # ==========================

      - name: Prepare input
        run: |
          mkdir -p input
          mkdir -p output
          mkdir -p output/clips


      # ==========================
      # CREATE YOUTUBE COOKIES
      # ==========================

      - name: Create YouTube cookies
        env:
          YOUTUBE_COOKIES: ${{ secrets.YOUTUBE_COOKIES }}

        run: |
          if [ -z "$YOUTUBE_COOKIES" ]; then
            echo "ERROR: YOUTUBE_COOKIES secret is not set"
            exit 1
          fi

          printf '%s' "$YOUTUBE_COOKIES" > youtube_cookies.txt

          chmod 600 youtube_cookies.txt

          echo "Cookies file created"

          if head -n 1 youtube_cookies.txt | grep -qE '^# (Netscape HTTP Cookie File|HTTP Cookie File)$'; then
            echo "Cookie format: OK"
          else
            echo "WARNING: Cookie file header was not detected"
          fi

          echo "Cookie file size:"
          wc -c youtube_cookies.txt


      # ==========================
      # DOWNLOAD SOURCE VIDEO
      # ==========================

      - name: Download source video
        env:
          SOURCE_URL: ${{ secrets.SOURCE_URL }}

        run: |
          if [ -z "$SOURCE_URL" ]; then
            echo "ERROR: SOURCE_URL secret is not set"
            exit 1
          fi

          yt-dlp \
            --cookies youtube_cookies.txt \
            --js-runtimes deno \
            --remote-components ejs:github \
            --no-playlist \
            -f "bv*+ba/b" \
            --merge-output-format mp4 \
            -o "input/source.%(ext)s" \
            "$SOURCE_URL"

          echo "Downloaded files:"
          ls -lah input/


      # ==========================
      # REMOVE COOKIES
      # ==========================

      - name: Remove YouTube cookies
        if: always()
        run: |
          rm -f youtube_cookies.txt


      # ==========================
      # RUN AI SHORTS GENERATOR
      # ==========================

      - name: Run AI
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          PEXELS_KEY: ${{ secrets.PEXELS_KEY }}
          FREESOUND_API_KEY: ${{ secrets.FREESOUND_API_KEY }}
          YOUTUBE_TOKEN: ${{ secrets.YOUTUBE_TOKEN }}

        run: |
          python main.py


      # ==========================
      # SAVE GENERATED FILES
      # ==========================

      - name: Save generated files
        uses: actions/upload-artifact@v4

        with:
          name: youtube-short

          path: |
            output/short.mp4
            output/thumbnail.jpg
            output/clips/*.mp4
            output/source_audio.wav

          if-no-files-found: warn
